import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-popover-v4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
